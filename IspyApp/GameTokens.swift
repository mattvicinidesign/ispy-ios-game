import SpriteKit

enum GameTokens {
    enum Colors {
        static let background = SKColor.systemTeal
        static let textPrimary = SKColor.white
    }

    enum Typography {
        static let titleFont = "AvenirNext-Bold"
        static let titleSize: CGFloat = 32
    }
}
